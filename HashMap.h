#include <iostream>
using namespace std;

class HashMap{

  vector<vector<pair<int,int>> map(100000, vector<pair<int,int>>);
  HashMap(){
  }
  
  int hashFunction(const int& key) const;
  bool insert(const int& key, const int& value);
  int find(const int& key);
  
};